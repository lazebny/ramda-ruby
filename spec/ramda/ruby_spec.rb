require "spec_helper"

RSpec.describe Ramda::Ruby do
  it "has a version number" do
    expect(Ramda::Ruby::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
